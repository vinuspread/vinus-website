@AGENTS.md

# 커밋 규칙

## Git Author

이 프로젝트의 커밋 author는 반드시 아래로 유지한다:

- `user.name`: `vinuspread`
- `user.email`: `vinus@vinus.co.kr`

커밋 전에 반드시 확인:

```bash
git config user.name && git config user.email
```

다른 author(예: 한성영 / hansungyoung@gmail.com)로 커밋하지 않는다.
